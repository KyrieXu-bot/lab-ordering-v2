# Windows PDF 转换节点。复制为 .env.pdf-worker 后填写随机长密钥。
PDF_WORKER_HOST=127.0.0.1
PDF_WORKER_PORT=4317
PDF_WORKER_API_KEY=replace_with_a_long_random_secret
PDF_WORKER_TIMEOUT_MS=120000
PDF_WORKER_MAX_FILE_SIZE_MB=20
